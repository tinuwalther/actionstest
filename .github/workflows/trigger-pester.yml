name: Trigger Pester Tests -> run Pester Tests and upload results

# Controls when the workflow will run
on: 
#  push:
#    branches-ignore:
#    - 'main'

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'branch name to checkout'
        # Default value if no value is explicitly provided
        default: 'main'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  trigger_pester:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: pwsh
        
    steps:
      - name: Clone repo ${{ github.event.inputs.name }}
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.inputs.name }}

      - name: Test with Pester
        run: |
          $ParentLocation = Get-Location
          cd Tests
          Write-Host "Tests: $(Get-Location | Select -ExpandProperty Path)"
          Install-Module -Name psyml, pshtml -force
          Import-Module -Name Pester -MinimumVersion 5.3.3
          $config = [PesterConfiguration]::Default
          $config.Filter.Tag = "Required"
          $config.Output.Verbosity = 'Detailed'
          $config.Run.PassThru = $true
          $NUnitReport = Invoke-Pester -Configuration $config

          $ReportsPath = Join-Path -Path $ParentLocation.FullName -ChildPath 'Reports'
          Write-Host $ReportsPath
          if(-not(Test-Path $ReportsPath)){
            $null = New-Item -Path $ReportsPath -ItemType Directory
          }
          cd $ReportsPath
          Write-Host "Reports: $(Get-Location | Select -ExpandProperty Path)"
          $NUnitReport | Export-NUnitReport -Path Last-TestsReport.NUnitXml
          
          $BinPath = Join-Path -Path $ParentLocation.FullName -ChildPath 'Bin'
          cd $BinPath
          Write-Host "Root: $(Get-Location | Select -ExpandProperty Path)"
          ./New-PesterReport.ps1

      - name: Upload Pester Result
        run: |
          git config --global user.email "it@martin-walther.ch"
          git config --global user.name "Martin Walther"
          git add .
          git commit -m "Upload Last-TestsReport.NUnitXml"
          git push

      - name: Dump GitHub context
        id: github_context_step
        run: echo '${{ toJSON(github) }}'
      #if: ${{ always() }}

      - name: Dump job context
        run: echo '${{ toJSON(job) }}'

      - name: Dump steps context
        run: echo '${{ toJSON(steps) }}'
